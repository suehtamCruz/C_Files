#include<stdio.h>


                // //Construa uma função rev que modique a
                // string de saída da seguinte forma,
                // 1. Modicar os caracteres alfabéticos minúsculos
                // pata maiores;
                // 2. Modicar os caracteres alfabéticos maiúsculos
                // para minúsculos+ /item Manter os
                // caracteres não-alfabéticos como são.
int main(void){
    char pal[3];
    pal[0] = 'w';  
    pal[1] = 'F';  
    pal[2] = '&';  
    printf("%c" , 23);
    return 0;
}